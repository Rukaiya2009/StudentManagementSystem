@model IEnumerable<StudentManagementSystem.Models.AssignmentReminder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AssignmentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReminderDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsSent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AssignmentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReminderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsSent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AssignmentReminderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AssignmentReminderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AssignmentReminderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

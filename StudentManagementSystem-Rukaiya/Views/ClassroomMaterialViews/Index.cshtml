@model IEnumerable<StudentManagementSystem_Rukaiya.Models.ClassroomMaterialView>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaterialId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ViewDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaterialId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ViewDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClassroomMaterialViewId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClassroomMaterialViewId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClassroomMaterialViewId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
